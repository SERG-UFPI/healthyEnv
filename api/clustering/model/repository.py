class Repository:
  def __init__(self, name, language, loc, stars, forks, open_issues, developers, commits):
    self.name = name
    self.language = language
    self.loc = loc
    self.stars = stars
    self.forks = forks
    self.open_issues = open_issues
    self.developers = developers
    self.commits = commits